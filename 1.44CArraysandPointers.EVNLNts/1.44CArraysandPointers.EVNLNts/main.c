#include<stdio.h>



int main() {


	//Declare an uninitialized int variable to act as a loop counter.
	//CODE:

	//Declare an integer array meatBalls with 5 elements and initialize them to some random values.
	//CODE:

	//Create for loop that initializes i to 0, a condition that i is less than 5 increment i by 1
	//CODE:

		//Call a print() that will use a sentence in the first argument, the value
		//of the current iteration i digit in second arg, the  memory address of each meatball 
		//element in 3rd argument and the actual value of each meatball element in the 4th argument.
		//CODE:

		//Array name by itself acts as a pointer to its first element. Here, memory address of meatBalls[0].
	//Call a print() to demonstrate that the meatballs array is memory address of the first element.
	//CODE:

	//Call a print() that gives the int value of meatballs by using *.
	//CODE:

	//Call printf(%p) that adds two to the array (3rd element of meatBalls) and shows memory 
	//address at that location. REMEMBER memory address in first arg is ALWAYS %p
	//CODE:

	//Call printf() wrapping () meatBalls + and make sure you dereference it to show the actual value of the 3rd position.
	//CODE:

}