#include<stdio.h>
#include<stdlib.h>



int main()
{
	//Declare an uninitialized int variable to store the user's age.
	//CODE:

	//Declare an uninitialized character variable to store the user's gender.
	//CODE:

	//Prompt the user to enter their age.
	//CODE:
	printf("Please enter you age.\n");

	//Call scanf_s() to store age value in int age.
	//CODE:

	//Prompt user for their gender using a call printf function.
	//CODE:
	printf("What is your gender?\n");

	//Call scanf_s() to scan in user data that has 3 parameters. '1' makes sure user ONLY 
	//enters 1 character 
	//CODE:

	//Create an outer if statement with the condition if the persons age is greater
	//than or equal to 18 to print the statement you may enter this website to the screen.
	//CODE:

		//Use nested if statement check if the user is male 'm' by setting the gender
		//variable == to m in the if condition. Then print() if male.
		//CODE:
		
		//Now use an if condition in the same fashion to check if the user is a female.
		//If they are write a sentence that a female has entered the website.
		//CODE:
			
		//Write if condition to test if the person is less than 18. If they
		//are print a statement that they cannot enter the website to the screen.
		//CODE:

}