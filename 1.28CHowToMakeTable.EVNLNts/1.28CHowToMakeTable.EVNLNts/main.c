#include<stdio.h>



//This program demonstrates how to print a simple 3X3 table using nested for loops

int main()

{

	//Declare two uninitialized variables one for row and one for column.
	//CODE:

	//Write/define an outer for loop that initializes the row to 1, then test if row is less than 4. 
	//iterate row by 1. 
	//CODE:

	//Write for loop to set column to 1, have the condition column < 4 and the increment by 1.
	//CODE:

	//Call a print() to show the current value (column variable) to the screen for each iteration.
	//CODE:

	//Print a new line here so the next row starts out on a new line.
	//CODE:

}