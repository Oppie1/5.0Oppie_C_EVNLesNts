#include<stdio.h>
#include<math.h>



int main() {

	//Declare two float variables that are initialized to two different decimal values.
	//CODE:

	//Call a printf() that in the first slot makes a sentence for float to 3 decimal places. The other argument being 
	//a call to the floor() with the first float as argument
	//CODE:

	//Call a print() that uses a sentence that matches the other argument being a call to the floor() to 5 dec places..
	//CODE:

	//Call the ceil() function now.
	//CODE:

	//Call the ceil() function on adamT2
	//CODE:

}

/*

*/