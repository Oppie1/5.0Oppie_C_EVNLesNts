#include<stdio.h>
#include<stdlib.h>


int main()
{

	//Declare three uninitialized integer variables to hold whole number values.
	//CODE:

	//Assign the same value (say 100) to those variables in a single statement by chaining the assignment
	//operator (=) from left to right.
	//CODE:

	//Print the values of a, b and c to the console. 
	//CODE:

	//Declare three float variables for 3 ages and 1 to store the result of average.
	//CODE:

	//Assign age1 and age2 equal to each other and the same value "10".
	//CODE:

	//Call a print function to prompt user to enter an age for age3.
	//CODE:
	printf("\nPlease enter your age:\n");

	//Call a scanf_s() function to read in age3 (remember to use &).
	//CODE:

//Calculate the average by summing all three ages and dividing by 3.
//Also, you will need to use "()" (age# + age )  because we divide by 3 -> order of operations.
//CODE:

//Call printf() to show a sentence with average out to the screen.
//CODE:

}