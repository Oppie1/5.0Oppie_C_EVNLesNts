#include <stdio.h>


int main() {

	//Declare two uninitialized int variables named peanuts and soybeans.
	//CODE:

	//Use a for loop to initialize peanut to 0. Have loop run so long as peanut is less than 10, increment by 2 each iteration.
	//CODE:

		//Show a sentence that says how many peanuts their are during each iteration.
		//CODE:

	printf("\nAnd now for the second example\n\n");

	//Create a for loop where soybean int value starts at 1. For the 
	//control condition use soybean <= 10 again but this time just increment by 1.
	//CODE:

		//Now same as above peanut example print out how many soybeans you have for all 10 iterations.
			//CODE:

}