#include<stdio.h>
#include<math.h>




int main() {

	//Declare an uninitialized int counter variable named i.
	//CODE:

	//Declare an uninitialized int variable named diceRoll to store that value.
	//CODE:

	//Create for loop with i initialized to 0, the condition i is less than 10, increments i by 1.
	//In codeblock Call printf() with digit plus newline and rand() with no parameters
	//CODE:

	printf("\n\nPrint a Random Dice Roll to the Screen (1-10 total rolls:) \n");

	//Create a for loop that initializes i to 5; a condition i < 10 and increments i by 1
	//CODE:

		//Assign the variable dice roll to the expression using the rand() function with no parameters modulus % 6
		//grouped together (expression) and then add 1 to it afterwards.
		//CODE:
		
		//Call a printf() with sentence and new line character that corresponds to the second argument being the variable diceRoll.
		//CODE:
	}
