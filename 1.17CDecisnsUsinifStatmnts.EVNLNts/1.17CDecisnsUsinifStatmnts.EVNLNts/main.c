#include<stdio.h>
#include<stdlib.h>	


int main() {

	//Declare an uninitialized int variable called "age" to hold the user's input.
	//CODE:

	//Call a print() to a prompt the user for their age. 
	//CODE:

	//Read the int the user types and store it in the variable "age" which passes the 
	//memory address of "age" to scanf_s, telling it exactly where in memory to write the value.
	//CODE:

	//Use an if statement to validate whether the person is greater than or equal to 18.
	//If they are call a print() to say they may enter the website to the screen.
	//CODE:

	//Create an if statement that does this along with a corresponding sentence 
	//in the if block that displays to screen.
	//CODE:

}