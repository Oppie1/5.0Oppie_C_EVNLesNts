#include<stdio.h>
#include<math.h>



int main() {

	//Declare an uninitialized int var for counter.
	//CODE:

	//Declare an int array named meatBalls with 5 uninitialized elements.
	//CODE:

	//Declare an int var called totalMeatBalls and initialize it to "0"
	//CODE:

	//Use a for loop initialize i to 0, the less than 5 and increment by 1
	//CODE:

	//Call a printf() to ask user how many meatballs they ate on day i (remember to add 1 since i 0)

	//Call a scanf_s() to scan in the user input for EACH day.

	//Create for loop just like previous one that now scroll though each elements/i value.
	//CODE:

	//Assign "totalMeatBalls" to expression of each user entry.
	//CODE:

	//DECLARE an initialized int var named avg that will be assigned a value through total meatballs variable and divides it by
	//number of days (i)/number of iterations.
	//CODE:

	//Print to screen a sentence that matches up with  totalMeatBalls as well as avg respectively in each of the two arguments.
	//CODE:

}