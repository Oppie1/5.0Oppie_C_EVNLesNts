#include<stdio.h>
#include<ctype.h>



int main() {

	//Declare an int variable named exampleDataType and initialize it to 'g'. 
	//CODE:

	//Use an if statement with the isalpha() function that has example data type variable as parameter.
	//CODE:

		//Use an NESTED if statement with isupper() function that has exampleDataType
		//as its parameter to test if it is an uppercase letter and print statement covering
		//both properties.
		//CODE:


		//Use an else statement still inside the outer if statement but outside
		//the inner if to say that _ is a lowercase letter.
		//CODE:

	//Use an else if statement with isdigit() and exampleDataType parameter to test if exampleDataType is digit
	//and printf corresponding message.
	//CODE:

		//Use else statement that will cover if it is not a letter or a number/ Print message out to screen.
		//CODE: 
}