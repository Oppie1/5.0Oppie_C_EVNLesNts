#include<stdio.h>
#include<stdlib.h>



//Declare a function prototype with int data type named calculateBonus with
//which takes an int named yearsWorked as its parameter.
//CODE:

int main() {

	//Declare variable named kanopisBonus and initialize it to the function calculate bonus with 14 as its arg.
	//CODE:

	//Declare and initialize int variable named petraBonus equal to the function calculate bonus with 3 as its arg.
	//CODE:

	//Print Kanopi's bonus to the screen.
	//CODE:

	//Print Petra's bonus to the screen.
	//CODE:

}

//Define the calculate bonus function,
//CODE:


	//Declare and assign int variable bonus to show what the bonus is for years worked by by 250

	//Use a nested if statement with the condition yearsWorked is greater than 10 they get 10000 more dollars.
	//CODE:

	//Use a return variable bonus to return the final bonus to the call function in main.
	//CODE:
