#include<stdio.h>
#include<math.h>



int main()
{

	//Call a printf() that shows a decimal variable to 5 decimals to screen with corresponding sqrt()
	//CODE:

	//Call a printf() and call power ->pow() with two comma separated values representing
	//the base x and raised to the power/exponent.
	//CODE:

}