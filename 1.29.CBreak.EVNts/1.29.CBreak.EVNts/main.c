#include<stdio.h>

int main() {

	//Declare 3 int variables the first two uninitialized.  i a counter variable for for loop, 
	//howMany -> which will determine if loop should stop before control section
	//Lastly maxAmount which will will be initialized to 10 - iterations program can have. 
	//Code:

	//Prompt user asking how many times do they want loop to loop (up to 10)
	//CODE:
	printf("How many times do you want this loop to loop? (up to 10)\n");

	//Scan in the user int selection. Then use the memory address of howMany as the storage for the input.
	//CODE:

	//Write a for loop that uses a as the counter and initialize it to 1. Then test
	//a against the maxAmount to see if it is less than or equal to 10. Then increment
	//i by 2 each iteration.
	//CODE:

		//print the current value of i on its own line each iteration.
		//CODE:

		//Check whether i has reached the user-specified limit using a if statement.
		//set a == howMany. When true, break exits the loop immediately.
		//CODE:
	
}